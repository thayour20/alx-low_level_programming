#include "main.h"

/**
 * _islower - to check if a char is a lower case
 * @c: char to check if is lower
 * Return: if is lower 0 and if is not 1
 */

int _islower(int c)
{
	if (c == 'C')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
