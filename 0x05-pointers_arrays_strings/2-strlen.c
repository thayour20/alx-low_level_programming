#include "main.h"

/**
 * _strlen - length of int
 * @s:String pointer to the string whose length is to be found
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int p = 0;

	while (*(s + p) != 0)
	{
		p++
	}
	return (p);
}
