#include <stdio.h>

/**
 * main - print alphabelt in lower cse
 * Return: always 0
 */

int main(void)
{

	char al;

	for (al = 'a'; al <= 'z'; al++)

	{
		putchar(al);

	}

	putchar('\n')


	return (0);
}
